name: Deploy Auth (Production)

description: Deploys to production server via self-hosted GitHub Actions Runner when main is updated

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to Production
    runs-on: self-hosted
    steps:
      - name: Pull latest changes
        run: |
          cd /home/homelab/selfhosted/dev/backend/auth
          git fetch origin
          git reset --hard origin/main
          echo "✅ Code pulled from main branch"

      - name: Build and deploy production container
        run: |
          docker compose -f /home/homelab/selfhosted/docker-compose/auth/docker-compose.yml up -d --build
          echo "🚀 Production container built and deployed"

      - name: Cleanup dangling images
        run: docker image prune -f